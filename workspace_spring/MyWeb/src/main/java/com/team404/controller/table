--------- board Table ---------
CREATE TABLE testBoard(
    num NUMBER(10),
    name VARCHAR2(50),
    title VARCHAR2(50),
    content VARCHAR2(100)
);

ALTER TABLE testBoard add CONSTRAINT testBoard_pk PRIMARY KEY(num);

CREATE SEQUENCE testBoard_seq START WITH 1 INCREMENT BY 1 NOCACHE;